(function(){
if(window.__cvQaHardeningInstalled)return;window.__cvQaHardeningInstalled=true;
const SPECIAL=new Set(['Livestock Guardian Dog','Horse']);
function permanentTag(a){return a&&a.tag&&a.tag!=='N-T'?String(a.tag).trim().toLowerCase():''}
function duplicateTags(){const counts=new Map();for(const a of cattle||[]){const k=permanentTag(a);if(k)counts.set(k,(counts.get(k)||0)+1)}return new Set([...counts].filter(([,n])=>n>1).map(([k])=>k))}
function matchesTag(tag){const k=String(tag||'').trim().toLowerCase();if(!k)return[];return (cattle||[]).filter(a=>permanentTag(a)===k)}
function firstPhoto(animalId){return new Promise(resolve=>{if(typeof db==='undefined'||!db)return resolve('');try{const store=db.transaction('media','readonly').objectStore('media'),idx=store.index('animalId'),cur=idx.openCursor(IDBKeyRange.only(String(animalId)));cur.onsuccess=e=>{const c=e.target.result;if(!c)return resolve('');const v=c.value;if(v?.type==='photo'&&v.data)return resolve(v.data);c.continue()};cur.onerror=()=>resolve('')}catch{return resolve('')}})}
async function thumbnailMap(rows){const out=new Map();for(let i=0;i<rows.length;i+=10){const batch=rows.slice(i,i+10);const vals=await Promise.all(batch.map(async a=>[String(a.id),await firstPhoto(a.id)]));for(const [id,p] of vals)if(p)out.set(id,p);await new Promise(r=>setTimeout(r,0))}return out}
function icon(a){if(a.sex==='Livestock Guardian Dog')return'🐕';if(a.sex==='Horse')return'🐎';return'🐄'}
let seq=0;
async function renderHardened(){const mine=++seq,q=$('search')?.value.trim().toLowerCase()||'';if(typeof lastHerdQuery!=='undefined'&&q!==lastHerdQuery){lastHerdQuery=q;herdLimit=60}const core=(cattle||[]).filter(a=>!SPECIAL.has(a.sex));if($('totalN'))$('totalN').textContent=core.length;if($('calfN'))$('calfN').textContent=core.filter(a=>['Calf','Heifer'].includes(a.sex)).length;if($('cowN'))$('cowN').textContent=core.filter(a=>a.sex==='Cow').length;if($('bullN'))$('bullN').textContent=core.filter(a=>a.sex==='Bull').length;const herd=$('herd');if(!herd)return;const collapsed=localStorage.getItem('cv2-herd-collapsed')!=='0';if(collapsed&&!q){herd.innerHTML='';return}const list=(cattle||[]).filter(a=>[a.tag,a.breed,a.sire,a.dam,a.location,a.notes,a.sex].join(' ').toLowerCase().includes(q));if(!list.length){herd.innerHTML='<div class="empty">No animals match this search.</div>';return}const limit=typeof herdLimit==='number'?herdLimit:60,shown=list.slice(0,limit),dupes=duplicateTags(),photos=await thumbnailMap(shown);if(mine!==seq)return;let html='';for(const a of shown){const ph=photos.get(String(a.id)),ic=icon(a),dup=dupes.has(permanentTag(a));html+=`<div class="animal" onclick="openRecord('${a.id}')">${ph?`<img class="thumb" loading="lazy" src="${ph}">`:`<div class="thumb" style="display:grid;place-items:center;font-size:35px">${ic}</div>`}<div><h3>${ic} Tag ${esc(a.tag||'N-T')}</h3><div class="chips"><span class="chip">${esc(a.sex)}</span>${a.breed?`<span class="chip">${esc(a.breed)}</span>`:''}${a.location?`<span class="chip">📍 ${esc(a.location)}</span>`:''}<span class="chip">${esc(a.status)}</span>${dup?'<span class="chip" style="background:#fff0cf;color:#805600">Duplicate tag — review</span>':''}</div>${a.dam?`<div>Dam: ${esc(a.dam)}</div>`:''}${a.sire?`<div>Sire: ${esc(a.sire)}</div>`:''}${a.notes?`<div class="muted">${esc(a.notes)}</div>`:''}</div><div class="arrow">›</div></div>`}if(shown.length<list.length)html+=`<div class="notice" style="text-align:center;margin-top:10px">Showing ${shown.length} of ${list.length} animals.<br><button type="button" class="softbtn" style="margin-top:8px" onclick="loadMoreHerd()">Load 60 More</button></div>`;herd.innerHTML=html}
window.render=renderHardened;
const oldTagOptions=window.tagOptions;if(typeof oldTagOptions==='function')window.tagOptions=function(){const seen=new Set(),rows=[];for(const a of (cattle||[]).slice().sort((a,b)=>String(a.tag).localeCompare(String(b.tag),undefined,{numeric:true}))){const k=permanentTag(a);if(!k||seen.has(k))continue;seen.add(k);rows.push(`<option value="${esc(a.tag)}">`)}return rows.join('')};
const originalBulkSave=window.saveCurrentBulkPhoto;if(typeof originalBulkSave==='function')window.saveCurrentBulkPhoto=async function(){const raw=$('bulkCurrentTag')?.value.trim()||'',m=matchesTag(raw);if(raw&&m.length>1){alert(`Tag ${raw} exists on ${m.length} separate animal records. Open My Herd and resolve the duplicate before attaching this photo so it does not go to the wrong animal.`);return}return originalBulkSave.apply(this,arguments)};
document.addEventListener('input',e=>{if(e.target?.id!=='bulkCurrentTag')return;const raw=e.target.value.trim(),m=matchesTag(raw),s=$('bulkStatus'+(typeof bulkIndex==='number'?bulkIndex:0)),b=$('bulkSaveOne');if(m.length>1){if(s)s.textContent=`Duplicate tag: ${m.length} records use Tag ${raw}. Resolve it before saving.`;if(b)b.disabled=true}else if(b)b.disabled=false},true);
const oldShow=window.showQuick;if(typeof oldShow==='function')window.showQuick=function(kind){const out=oldShow.apply(this,arguments);if(kind==='reports'){const d=[...duplicateTags()];if(d.length&&$('qBody'))$('qBody').insertAdjacentHTML('beforeend',`<div class="history" style="background:#fff8dc"><b>${d.length} duplicate permanent tag${d.length===1?'':'s'} need review</b><br>${d.map(x=>esc(x.toUpperCase())).join(', ')}</div>`)}return out};
function addSettingsBackupButtons(){const sheet=$('settingsModal')?.querySelector('.sheet');if(!sheet||$('cvLocalBackupBtn'))return;const cloud=[...sheet.querySelectorAll('button')].find(b=>b.textContent.includes('Open Cloud'));if(!cloud)return;const box=document.createElement('div');box.id='cvLocalBackupBox';box.innerHTML=`<button id="cvLocalBackupBtn" class="softbtn" style="width:100%;margin-bottom:10px" onclick="backup()">Download Local Record Backup</button><button class="softbtn" style="width:100%;margin-bottom:8px" onclick="restoreInput.click()">Restore Local Record Backup</button><div class="muted" style="font-size:12px;margin-bottom:12px">The local backup includes animal, pasture, medication, and herd-work records. Photos and videos are protected through Cattle Vision Cloud, not the JSON backup file.</div>`;cloud.insertAdjacentElement('afterend',box)}
addSettingsBackupButtons();
setTimeout(()=>{addSettingsBackupButtons();renderHardened()},50);
})();