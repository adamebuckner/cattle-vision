(()=>{let newAnimalPhotos=[];let previewURL='';function setupPhotoFirst(){const add=document.getElementById('add');if(!add||document.getElementById('addAnimalPhotoInput'))return;const panel=document.createElement('div');panel.className='add-photo-first';panel.innerHTML=`<h3>📷 Start with a Photo</h3><div class="photo-first-note">Choose or take a photo first so you can zoom in and read the ear tag before entering the animal's information.</div><div class="action-row"><button type="button" class="primary" id="addTakePhoto">📷 Take Photo</button><button type="button" class="secondary" id="addChoosePhoto">🖼 Choose Photo</button></div><input id="addAnimalPhotoInput" type="file" accept="image/*" style="display:none"><input id="addAnimalCameraInput" type="file" accept="image/*" capture="environment" style="display:none"><img id="addAnimalPhotoPreview" class="add-photo-preview" alt="Selected animal photo"><div id="addPhotoHint" class="small"></div>`;const title=add.querySelector('h2');title.insertAdjacentElement('afterend',panel);const choose=document.getElementById('addAnimalPhotoInput'),camera=document.getElementById('addAnimalCameraInput');document.getElementById('addChoosePhoto').onclick=()=>choose.click();document.getElementById('addTakePhoto').onclick=()=>camera.click();choose.onchange=e=>selectNewAnimalPhoto(e.target.files);camera.onchange=e=>selectNewAnimalPhoto(e.target.files)}function selectNewAnimalPhoto(files){newAnimalPhotos=Array.from(files||[]).filter(f=>f.type.startsWith('image/'));if(previewURL)URL.revokeObjectURL(previewURL);const img=document.getElementById('addAnimalPhotoPreview'),hint=document.getElementById('addPhotoHint');if(!newAnimalPhotos.length){img.classList.remove('show');hint.textContent='';return}previewURL=URL.createObjectURL(newAnimalPhotos[0]);img.src=previewURL;img.classList.add('show');hint.textContent='Photo ready. Zoom in if needed, read the tag, then enter the animal information below.'}const originalHide=window.hideAdd;window.hideAdd=function(){newAnimalPhotos=[];if(previewURL){URL.revokeObjectURL(previewURL);previewURL=''}const img=document.getElementById('addAnimalPhotoPreview');if(img){img.src='';img.classList.remove('show')}const hint=document.getElementById('addPhotoHint');if(hint)hint.textContent='';if(originalHide)originalHide()};window.addAnimal=async function(){const t=document.getElementById('tag').value.trim();if(!t){alert('Enter a tag number.');return}if(cattle.some(a=>String(a.tag).toLowerCase()===t.toLowerCase())){alert('That tag number already exists.');return}const animal=normalizeAnimal({id:makeId(),tag:t,sex:document.getElementById('sex').value,breed:document.getElementById('breed').value.trim(),birth:document.getElementById('birth').value,location:document.getElementById('location').value.trim(),registration:document.getElementById('registration').value.trim(),dam:document.getElementById('dam').value.trim(),sire:document.getElementById('sire').value.trim(),notes:document.getElementById('notes').value.trim(),created:new Date().toISOString()});cattle.push(animal);localStorage.setItem('cattleVision',JSON.stringify(cattle));let photoSaved=false;if(newAnimalPhotos.length&&photoDB){try{await savePhotoFiles(newAnimalPhotos,animal.id);photoSaved=true}catch(e){console.error(e)}}['tag','breed','birth','location','registration','dam','sire','notes'].forEach(id=>document.getElementById(id).value='');window.hideAdd();save();if(newAnimalPhotos.length&&!photoSaved)alert('Animal saved. The selected photo could not be stored in this browser.');else alert(photoSaved?'Animal and photo saved.':'Animal saved.')};function addFooter(){if(document.querySelector('.app-footer'))return;const f=document.createElement('div');f.className='app-footer';f.textContent='Cattle Vision • Your herd. Your records. Your operation.';document.body.appendChild(f)}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',()=>{setupPhotoFirst();addFooter()});else{setupPhotoFirst();addFooter()}})();