(function(){
const KEY='cv2-testing-notice-ack-v1';
function closeTestingNotice(){localStorage.setItem(KEY,'1');document.getElementById('testingNotice')?.classList.add('hidden')}
window.closeTestingNotice=closeTestingNotice;
function makeNotice(){if(document.getElementById('testingNotice'))return;const m=document.createElement('div');m.id='testingNotice';m.className='modal hidden';m.innerHTML=`<div class="sheet" style="max-width:620px"><h2 style="margin-top:0">Cattle Vision is currently in testing</h2><p>Your records still save on this phone/browser first. Cattle Vision Cloud is now being added as a beta backup and account system.</p><div style="padding:12px;border:1px solid #dfc56a;background:#fff8d8;border-radius:10px;margin:12px 0"><b>Please do not use this testing version as the only copy of irreplaceable farm records yet.</b><br><span class="muted">Until cloud migration has successfully completed, clearing browser data, losing or replacing the phone, or other device-storage changes could remove locally stored information.</span></div><p>You can now create a cloud account and farm, then use <b>Migrate / Sync This Device</b> to begin copying local cattle records and media to the cloud. The local copy is kept on the phone.</p><div style="padding:12px;border:1px solid #d5ddd6;border-radius:10px;margin:12px 0"><b>Still recommended during testing:</b><br>Use Backup periodically for your cattle record data and verify cloud migration finishes before relying on it.</div><button class="greenbtn" style="width:100%" onclick="closeTestingNotice()">Okay, I Understand</button></div>`;document.body.appendChild(m);if(localStorage.getItem(KEY)!=='1')m.classList.remove('hidden')}
function loadCloud(){if(document.querySelector('script[data-cloud-loader]'))return;const s=document.createElement('script');s.src='cloud-loader.js?v=3';s.dataset.cloudLoader='1';document.body.appendChild(s)}
makeNotice();loadCloud();
})();